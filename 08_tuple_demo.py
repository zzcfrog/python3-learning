#!/usr/bin/env python3
# -*- coding: utf-8 -*-

tup = (1, 2, 3, 4, 5)
print(tup)

# tuple 是不可变的，不能修改元素，其他都和 list 一样
# string, list, tuple 都属于序列类型，可以进行索引、切片、迭代等操作

# 空元组
tup1 = ()
print(tup1)

''' 特别注意 '''
# 只有一个元素的元组 
tup2 = (1,)